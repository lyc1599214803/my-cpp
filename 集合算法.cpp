/*
set_intersection   //求两个容器的交集
set_union          //求两个容器的并集
set_difference     //求两个容器的差集
*/


set_intersection(itrator beg1, iterator end1, iterator beg2, iterator end2, iterator dest);
set_union(itrator beg1, iterator end1, iterator beg2, iterator end2, iterator dest);
set_difference(itrator beg1, iterator end1, iterator beg2, iterator end2, iterator dest);//两集合必须为有序序列
